# Notion 캡처 자리

작업 지시서는 [`../make/README.md`](../make/README.md) 에 Make 와 함께 있다(계정·토큰이 한 벌이라 분리하지 않았다).

이 디렉터리에 넣을 것:

| 파일명 | 무엇이 보여야 하나 |
|---|---|
| `01-db.png` | DB **속성 열** + **실제로 저장된 행**이 같은 화면에. 빈 표는 채점을 통과하지 못한다 |

추가로 **공유 링크 1줄**을 루트 `README.md` 상단 진행상태 표에 넣는다.

> ⚠️ 캡처에 Notion Integration 토큰·API 키가 보이면 반드시 가린다(이 repo 는 public).
